import { colors } from "./colors";
import { fonts } from "./fonts";
import { mixins } from "./mixins";
import { media } from "./media";

export const theme = {
  colors,
  fonts,
  mixins,
  media,
};
